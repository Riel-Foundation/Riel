# This just prevents the binary file from being added to the repo,
# it should be placed in releases, and when installed later, in user's PATH
binary
# Generated by Cargo
# will have compiled files and executables
debug
target

# Remove Cargo.lock from Rielignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are some meta-things
.git
